{
  "buildCommand": "cd bored-app && npm install && npm run build",
  "outputDirectory": "bored-app/dist/bored-app/browser",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
